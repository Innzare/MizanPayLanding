# API Configuration
# Если не указать VITE_API_URL, будет использоваться mock авторизация
# VITE_API_URL=http://localhost:8000/api

# Development mode (автоматически устанавливается Vite)
# NODE_ENV=development
